---
import '../styles/global.css';
import { withBase } from '../lib/url';
const { title, profile } = Astro.props;
const base = import.meta.env.BASE_URL;
const nav = [
  ['nav.home', 'Home', withBase('/', base)],
  ['nav.paperPool', 'Paper Pool', withBase('/paper-pool/', base)],
  ['nav.deepRead', 'Deep Read', withBase('/deep-read/', base)],
  ['nav.dailyArchive', 'Daily Archive', withBase('/daily-archive/', base)],
  ['nav.favorites', 'Favorites', withBase('/favorites/', base)],
];
---
<!doctype html>
<html lang="zh-CN" data-locale="zh-CN" data-state-namespace={base}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={profile?.profile?.description || 'A reusable research library framework'} />
    <title>{title ? `${title} · ` : ''}{profile?.profile?.library_name || 'Research Library'}</title>
    <script is:inline>
      try {
        const locale = localStorage.getItem('research-library-locale') === 'en' ? 'en' : 'zh-CN';
        document.documentElement.dataset.locale = locale;
        document.documentElement.lang = locale;
      } catch {}
    </script>
  </head>
  <body>
    <header class="site-header">
      <a class="brand" href={withBase('/', base)} aria-label="Home"><span class="brand-mark">R/</span><span>{profile?.profile?.library_name || 'Research Library'}</span></a>
      <nav aria-label="Primary navigation">{nav.map(([key, label, href]) => <a href={href} data-i18n={key}>{label}</a>)}</nav>
      <div class="header-actions">
        <button class="locale-toggle" type="button" data-locale-toggle aria-label="Switch to Chinese">EN / ZH</button>
        <button class="theme-toggle" type="button" data-theme-toggle aria-label="Toggle dark mode">◐</button>
      </div>
    </header>
    <main><slot /></main>
    <footer class="site-footer"><div class="footer-meta"><span data-i18n="footer.content">结构化数据存储在 Git · 内容优先</span><span data-i18n="footer.generated">由 ChatGPT/Codex 生成</span></div><p class="research-disclaimer" data-i18n="footer.researchDisclaimer">学术文献整理与研究证据摘要，不提供个体诊断或医疗建议；研究性筛查模型不能替代临床评估。</p></footer>
    <script is:inline>
      const key = 'research-library-theme';
      const saved = localStorage.getItem(key);
      if (saved === 'dark' || (!saved && matchMedia('(prefers-color-scheme: dark)').matches)) document.documentElement.dataset.theme = 'dark';
      document.querySelector('[data-theme-toggle]')?.addEventListener('click', () => { const dark = document.documentElement.dataset.theme === 'dark'; document.documentElement.dataset.theme = dark ? 'light' : 'dark'; localStorage.setItem(key, dark ? 'light' : 'dark'); });
    </script>
    <script src="../scripts/library.js"></script>
  </body>
</html>
